{
  "name": "custom-gfn-client",
  "version": "1.0.0",
  "description": "A custom Electron client for GeForce NOW with a post-processing pipeline.",
  "main": "main.js",
  "scripts": {
    "start": "electron . --disable-frame-rate-limit --enable-gpu-rasterization",
    "build": "electron-builder"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "dependencies": {}
}
